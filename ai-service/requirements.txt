# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
httpx==0.27.2

# ── LLM providers ─────────────────────────────────────────────────────────────
openai==1.51.0          # OpenAI + Groq (OpenAI-compatible)
anthropic==0.34.2       # Anthropic Claude
google-generativeai==0.8.2  # Google Gemini
ollama==0.3.3           # Local Ollama (free, no API key needed)

# ── Embeddings & Vector DB ────────────────────────────────────────────────────
chromadb==0.5.15        # Local vector database
sentence-transformers==3.1.1  # Local embeddings (no API key needed)
tiktoken==0.7.0         # Token counting

# ── Document processing ───────────────────────────────────────────────────────
pypdf==4.3.1            # PDF text extraction
python-docx==1.1.2      # DOCX text extraction
chardet==5.2.0          # Encoding detection

# ── Safety / Moderation ───────────────────────────────────────────────────────
better-profanity==0.7.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
numpy==1.26.4
tenacity==9.0.0         # Retry logic with exponential backoff
